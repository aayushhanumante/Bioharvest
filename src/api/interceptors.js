// src/api/interceptors.js - Request/response interceptors
// Placeholder for future interceptors
export const requestInterceptor = (config) => {
  // Add logging, auth, etc.
  return config;
};

export const responseInterceptor = (response) => {
  // Handle responses
  return response;
};